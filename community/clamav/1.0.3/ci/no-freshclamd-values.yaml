clamavStorage:
  sigdb:
    type: hostPath
    hostPath: /mnt/{{ .Release.Name }}/sig-db
  scandir:
    type: hostPath
    hostPath: /mnt/{{ .Release.Name }}/scan-dir

clamavConfig:
  disableFreshClamd: true
