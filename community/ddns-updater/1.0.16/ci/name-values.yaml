ddnsStorage:
  data:
    type: hostPath
    hostPath: /mnt/{{ .Release.Name }}/data

# This disables probes.
# As we can't have for each provider
# an api key, we just run through CI
# for the linting.
ci: true

ddnsConfig:
  config:
    - provider: name
      host: subdomain
      domain: example.com
      ipVersion: ""
      nameUsername: "abcdef123456abcdef123456abcdef12"
      nameToken: "abcdef123456abcdef123456abcdef12"
      nameTTL: 300
    - provider: name
      host: subdomain
      domain: example.com
      ipVersion: ""
      nameUsername: "abcdef123456abcdef123456abcdef12"
      nameToken: "abcdef123456abcdef123456abcdef12"
      nameTTL: 300

