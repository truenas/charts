jellyfinNetwork:
  webPort: 31000

jellyfinStorage:
  config:
    type: pvc
  cache:
    type: pvc
  transcodes:
    type: emptyDir
    medium: ''
    size: ''

  additionalStorages:
  - type: pvc
    mountPath: /movies
  - type: pvc
    mountPath: /series
