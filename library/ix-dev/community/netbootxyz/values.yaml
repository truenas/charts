image:
  repository: netbootxyz/netbootxyz
  pullPolicy: IfNotPresent
  tag: 0.7.0-nbxyz2

resources:
  limits:
    cpu: 4000m
    memory: 8Gi

netbootConfig:
  additionalEnvs: []

netbootNetwork:
  tftpPort: 31009
  webHttpPort: 31010
  webAssetsPort: 31011
  # TFTP only works with hostNetwork: true
  hostNetwork: false

netbootStorage:
  config:
    type: ixVolume
    ixVolumeConfig:
      datasetName: config
  assets:
    type: ixVolume
    ixVolumeConfig:
      datasetName: assets
  additionalStorages: []
